import classicConfig from './classic';
import type { ConfigOptions } from './config';
import { config } from './config';

export { ConfigOptions, config, classicConfig as __internal_classicConfig };
export default config;
